*Regression equation from three columns of numerical data

DATA LIST LIST / v0 TO {VarCount} (F9.2).

BEGIN DATA.
{DataList}
END DATA.

EXAMINE VARIABLES=v0
     /STATISTICS = DESCRIPTIVES EXTREME(10)
     /CINTERVAL {ConfidenceInterval}
     /PLOT = HISTOGRAM

REGRESSION
	/VARIABLES= {VarList}
	/DEPENDENT= v0
	/METHOD=ENTER
	/STATISTICS=COEFF CI R ANOVA BCOV
	/SAVE= PRED RESID.
