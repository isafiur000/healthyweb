' Gambas class file

Static Public Sub _call(encid As String, Optional sBillMode As String) As Collection

  If sBillMode Then
    Return fmSetRegimen.Run(encid, sBillMode)
  Else
    Return fmSetRegimen.Run(encid)
  Endif

End
