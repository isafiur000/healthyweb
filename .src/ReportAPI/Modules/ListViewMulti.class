' Gambas class file

Static Public Sub _call(encid As String, Optional sYear As String) As Variant[]

  If sYear Then
    Return fmListViewMulti.Run(encid, sYear)
  Else
    Return fmListViewMulti.Run(encid, "")
  Endif

End
