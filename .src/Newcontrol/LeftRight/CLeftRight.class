' Gambas class file

Static Public Sub _call(strExam As String, strValue As String, Optional strHeader As String) As Variant[]

  If strHeader Then
    Return fmChooseRightLeft.Run(strExam, strValue, strHeader)
  Else
    Return fmChooseRightLeft.Run(strExam, strValue, "")
  Endif

End
