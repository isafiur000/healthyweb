' Gambas class file

Static Public Sub _call(encid As String, sPresent As Boolean) As String[]                             ''

  If modPatient.GetPatientSex(encid) = "Female" Then
    Return fmDiagnoLabor.Run(encid, sPresent)
  Endif

End
