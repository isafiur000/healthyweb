' Gambas class file

Static Public Sub _call(dept As String, Optional sTime As Date) As Date                              ''

  Dim xx As Date

  If sTime Then
    xx = sTime
  Else
    xx = Now()
  Endif
  Return fmTimeCons.Run(dept, xx)

End