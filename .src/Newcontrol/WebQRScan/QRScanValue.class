' Gambas class file

Static Public Sub _call(sDevice As String) As String

  If modPrint.GetHTTPReferer() = "https" Then
    Return fmQRScanValue.Run(sDevice)
  Else If modHelpVariable.$AllowInsecureDevice = "Yes" Then
    Return fmQRScanValue.Run(sDevice)
  Endif

End
